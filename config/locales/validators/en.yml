en:
  validators:
    academic_term:
      active_check: One of the academic terms must be active.
    activation_service:
      cannot_be_blank: If you have the old identity card, you must fill in the Serial and Serial No fields, the Document Number field if you have a chip identity card.
    address:
      max_formal: You can have up to %{limit} formal addresses maximum.
      max_informal: You can add up to %{limit} addresses maximum.
    available_course_lecturer:
      coordinator: There must be one coordinator of the available course group.
    course_evaluation_type:
      invalid_percentages: The percentage of the assessment methods must be equal to 100.
    datetime:
      after_or_eql: must be equal or after than %{restriction}
      after: must be after than %{restriction}
      before_or_eql: must be equal or before than %{restriction}
      before: must be before than %{restriction}
      comparison_value: Comparison value
      eql: must be eql %{restriction}
    duty:
      active_and_tenure: This user already has an active and tenure duty. Edit the existing duty or make it passive in order to create a new one.
      invalid_end_date: Start date is selected as a date after the end date
      multiple_active: This user has already active duty at the selected unit
    employee:
      active: This user already has an active employee. Edit the existing employee or make it passive in order to create a new one.
    identity:
      max_formal: You can have up to %{limit} identities related with your user account.
      max_informal: You can create up to %{limit} informal identities.
    image:
      not_permitted: '%{mime_type} is not an allowed file type. You can upload files in %{extension_whitelist} formats.'
      size_not_satisfied: File size is not between the permitted range. You can upload files from minimum %{minimum} KBs to maximum %{maximum} MBs.
    prospective_student:
      unchangeable_variables: The program and academic term of a registered student cannot be changed.
    position:
      invalid_end_date: Start date is selected as a date after the end date
      multiple_active_repetitive: This employee already has this position in this unit
    unit_tuition:
      defined: 'Tuition fee already defined for %{name}'
