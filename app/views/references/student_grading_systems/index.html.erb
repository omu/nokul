<div class='alert alert-light'>
  <%= link_to_new(new_student_grading_system_path, t('.new_student_grading_system_link')) %>
</div>

<div class='row'>
  <div class='col-lg-12'>
    <div class='card'>
      <div class='card-header'>
        <%= fa_icon 'align-justify', text: t('.card_header') %>
      </div>
      <div class='card-body'>
        <table class='table table-responsive-sm table-striped'>
          <thead>
            <tr>
              <th><%= t('.name') %></th>
              <th><%= t('.code') %></th>
              <th><%= t('actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% @student_grading_systems.each do |student_grading_system| %>
              <tr>
                <td><%= student_grading_system.name %></td>
                <td><%= student_grading_system.code %></td>
                <td>
                  <%= link_to_edit(edit_student_grading_system_path(student_grading_system)) %>
                  <%= link_to_destroy(student_grading_system) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <nav aria-label='pagination'>
      <ul class='pagination justify-content-center'>
        <%== pagy_nav_bootstrap(@pagy) %>
      </ul>
    </nav>
  </div>
</div>
