<div class='alert alert-light'>
  <%= link_to_new(new_evaluation_type_path, t('.new_evaluation_type_link')) %>
</div>

<div class='row'>
  <div class='col-lg-12'>
    <div class='card'>
      <div class='card-header'>
        <%= fa_icon 'th-list', text: t('.card_header') %>
      </div>
      <div class='card-body'>
        <table class='table table-responsive-sm table-striped'>
          <thead>
            <tr>
              <th>Adı</th>
              <th><%= t('actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% @evaluation_types.each do |evaluation_type| %>
              <tr>
                <td><%= evaluation_type.name %></td>
                <td>
                  <%= link_to_edit(edit_evaluation_type_path(evaluation_type)) %>
                  <%= link_to_destroy(evaluation_type) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
