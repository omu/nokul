<!DOCTYPE html>
<html>
  <head>
    <style>
      table, td, th {  
        border: 1px solid #ddd;
        text-align: left;
      }

      table {
        border-collapse: collapse;
        width: 100%;
        margin-top: 3em;
        margin-bottom: 3em;
      }

      th, td {
        padding: 15px;
      }

      thead { display: table-header-group }
      tfoot { display: table-row-group }
      tr { page-break-inside: avoid }

      #header {
        position: relative;
        width: 100%;
        height: 150px;
        margin-bottom: 40px;
      }

      #header h1 {
        position: absolute;
        max-width: 75%;
        text-align: center;
        margin: 0 auto;
        left: 0;
        right: 0;
        -ms-transform: translateY(-50%);
        transform: translateY(-50%);
      }

      #header #logo {
        position: absolute;
        left: 0;
        height: 96px;
        width: 96px;
        -ms-transform: translateY(-50%);
        transform: translateY(-50%);
      }

      #header #logo img {
        width: 96px;
        height: 96px;
      }
    </style>
  </head>
  <body>
    <div id="header">
      <h1><%= t('.university_name') %><br><%= t('.title') %></h1>
      <div id="logo">
        <img src="http://www.omu.edu.tr/sites/all/themes/anasayfa/images/Kurumsal/omu-logotr.jpg"/>
      </div>
      <div align="right">
        <img style="width: 124px; height: 35px; margin-bottom: -4em;" src="http://baum.omu.edu.tr/user/sites/baum.omu.edu.tr/pages/01.home/baum-245x40.png"/>
      </div>
    </div>
    <h2><%= t('.stats') %></h2>
    <table id="stats">
      <tr>
        <td><%= t('.indoor_area') %> (m<sup>2</sup>)</td>
        <td><%= number_to_human(@buildings.sum(:indoor_area)) %></td>
      </tr>
      <tr>
        <td><%= t('.number_of_buildings') %></td>
        <td><%= number_to_human(@buildings.count) %></td>
      </tr>
      <tr>
        <td><%= t('.number_of_active_buildings') %></td>
        <td><%= number_to_human(@buildings.actives.size) %></td>
      </tr>
      <tr>
        <td><%= t('.number_of_classrooms') %></td>
        <td><%= number_to_human(@classrooms.count) %></td>
      </tr>
      <tr>
        <td><%= t('.student_capacity') %></td>
        <td><%= number_to_human(@classrooms.sum(:student_capacity)) %></td>
      </tr>
      <tr>
        <td><%= t('.exam_capacity') %></td>
        <td><%= number_to_human(@classrooms.sum(:exam_capacity)) %></td>
      </tr>
    </table>
    <h2><%= t('.buildings_list') %></h2>
    <div class="alwaysbreak">
    <table id="buildings">
      <tr>
        <th><%= t('.name') %></th>
        <th><%= t('.code') %></th>
        <th><%= t('.place_type') %></th>
        <th><%= t('.unit') %></th>
        <th><%= t('.student_capacity') %></th>
        <th><%= t('.exam_capacity') %></th>
        <th><%= t('.indoor_area') %> (m<sup>2</sup>)</th>
      </tr>
      <% @buildings.each do |building| %>
        <tr>
          <td><%= building.name %></td>
          <td><%= building.code %></td>
          <td><%= building.place_type.name %></td>
          <td><%= building.unit.name %></td>
          <td><%= number_to_human(building.classrooms.sum(:student_capacity)) %></td>
          <td><%= number_to_human(building.classrooms.sum(:exam_capacity)) %></td>
          <td><%= number_to_human(building.indoor_area) %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
