<div class='row'>
  <div class='col-lg-12'>
    <div class='card'>
      <div class='card-header'>
        <%= fa_icon 'align-justify', text: t('.card_header') %>
        <div class='card-header-actions'>
          <%= link_to_new(t('.new_accreditation_standard_link'), new_accreditation_standard_path) %>
        </div>
      </div>
      <div class='card-body'>
        <table class='table table-responsive-sm table-sm'>
          <%= render 'search' %>
          <thead>
            <tr>
              <th><%= t('.accreditation_institution') %></th>
              <th><%= t('.version') %></th>
              <th><%= t('.units') %></th>
              <th><%= t('.status') %></th>
              <th><%= t('actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% @accreditation_standards.each do |accreditation_standard| %>
              <tr>
                <td><%= accreditation_standard.name %></td>
                <td><%= accreditation_standard.version %></td>
                <td style='width: 60%'><%= accreditation_standard.units.map(&:names_depth_cache).join(', ') %></td>
                <td><%= enum_t(accreditation_standard, :status) %></td>
                <td><%= link_to_actions(accreditation_standard) %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <nav aria-label='pagination'>
      <ul class='pagination justify-content-center'>
        <%== pagy_bootstrap_nav(@pagy) %>
      </ul>
    </nav>
  </div>
</div>
