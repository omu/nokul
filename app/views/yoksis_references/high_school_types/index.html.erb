<div class='alert alert-light'>
  <%= link_to_new(new_high_school_type_path, t('.new_high_school_type_link')) %>
</div>

<div class='row'>
  <div class='col-lg-12'>
    <div class='card'>
      <div class='card-header'>
        <%= fa_icon 'align-justify', text: t('.card_header') %>
      </div>
      <div class='card-body'>
        <table class='table table-responsive-sm table-stripe'>
          <thead>
            <tr>
              <th><%= t('.name') %></th>
              <th><%= t('.code') %></th>
              <th><%= t('actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% @high_school_types.each do |high_school_type| %>
              <tr>
                <td><%= high_school_type.name %></td>
                <td><%= high_school_type.code %></td>
                <td>
                  <%= link_to_edit(edit_high_school_type_path(high_school_type)) %>
                  <%= link_to_destroy(high_school_type) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <nav aria-label='pagination'>
      <ul class='pagination justify-content-center'>
        <%== pagy_nav_bootstrap(@pagy) %>
      </ul>
    </nav>
  </div>
</div>
