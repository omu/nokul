<%= render 'layouts/builders/form',
           namespace: 'admin',
           klass: @language,
           params: [
             {
               field: 'name',
               width: 12,
               required: true
             },
             {
               field: 'iso',
               width: 12,
               required: true
             }
           ] %>
