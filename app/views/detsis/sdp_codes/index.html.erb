<div class='row'>
  <div class='col-lg-12'>
    <div class='card'>
      <div class='card-header'>
        <%= fa_icon 'align-justify', text: t('.card_header') %>
      </div>
      <div class='card-body'>
        <%= render 'search' %>
        <table class='table table-responsive-sm table-striped'>
          <thead class="thead-dark">
            <tr>
              <th><%= t('.name') %></th>
              <th><%= t('.main') %></th>
              <th><%= t('.first') %></th>
              <th><%= t('.second') %></th>
              <th><%= t('.third') %></th>
              <th><%= t('.full') %></th>
            </tr>
          </thead>
          <tbody>
            <% @paginated.each do |sdp_code| %>
              <tr>
                <td><%= sdp_code.name %></td>
                <td><%= sdp_code.main %></td>
                <td><%= sdp_code.first %></td>
                <td><%= sdp_code.second %></td>
                <td><%= sdp_code.third %></td>
                <td><%= sdp_code %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <nav aria-label='pagination'>
      <ul class='pagination justify-content-center'>
        <%== pagy_bootstrap_nav(@pagy) %>
      </ul>
    </nav>
  </div>
</div>
