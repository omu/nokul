<div class='alert alert-light'>
  <%= link_to_back(:back) %>
</div>

<div class="row">
  <div class="col-sm-12">
    <div class="card">
      <div class="card-header">
        <%= fa_icon 'calendar' %><strong><%= @student_entrance_point_type.name %></strong>
        <div class="card-header-actions">
          <%= link_to_edit(edit_student_entrance_point_type_path(@student_entrance_point_type)) %>
          <%= link_to_destroy(@student_entrance_point_type) %>
        </div>
      </div>

      <div class="card-body">
        <table class="table table-responsive-sm">
          <tbody>
            <tr>
              <td><%= t('activerecord.attributes.student_entrance_point_type.name') %></td>
              <td><%= @student_entrance_point_type.name %></td>
            </tr>
            <tr>
              <td><%= t('activerecord.attributes.student_entrance_point_type.code') %></td>
              <td><%= @student_entrance_point_type.code %></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
