<%= render 'layouts/builders/form',
           namespace: 'reference',
           klass: @language,
           params: [
             {
               field: 'name',
               width: 12,
               required: true
             },
             {
               field: 'iso',
               width: 12,
               required: true
             }
           ] %>
