<div class='alert alert-light'>
  <%= link_to_back(:back) %>
</div>

<div class="row">
  <div class="col-sm-12">
    <div class="card">
      <div class="card-header">
        <%= fa_icon 'calendar' %><strong><%= @student_education_level.name %></strong>
        <div class="card-header-actions">
          <%= link_to_edit(edit_student_education_level_path(@student_education_level)) %>
          <%= link_to_destroy(@student_education_level) %>
        </div>
      </div>

      <div class="card-body">
        <table class="table table-responsive-sm">
          <tbody>
            <tr>
              <td><%= t('activerecord.attributes.student_education_level.name') %></td>
              <td><%= @student_education_level.name %></td>
            </tr>
            <tr>
              <td><%= t('activerecord.attributes.student_education_level.code') %></td>
              <td><%= @student_education_level.code %></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
